// Generated by cimgen — do not edit by hand.
#![allow(clippy::all, dead_code, unused)]
use cimdecoder::CimDataset;
use crate::Violation;
use crate::helpers::*;

pub fn validate_p61970_456_statevariables_ap_con_complex_explicit_crossprofile_shacl(dataset: &CimDataset) -> Vec<Violation> {
    let mut v = Vec::new();
    v.extend(check_p61970_456_statevariables_ap_con_complex_explicit_crossprofile_shacl_dc_topological_island_etc(dataset));
    v.extend(check_p61970_456_statevariables_ap_con_complex_explicit_crossprofile_shacl_sv_injection_sv_inject_etc(dataset));
    v.extend(check_p61970_456_statevariables_ap_con_complex_explicit_crossprofile_shacl_sv_power_flow_sv_power_etc(dataset));
    v.extend(check_p61970_456_statevariables_ap_con_complex_explicit_crossprofile_shacl_sv_shunt_compensator_s_etc(dataset));
    v.extend(check_p61970_456_statevariables_ap_con_complex_explicit_crossprofile_shacl_sv_status_sv_status_co_etc(dataset));
    v.extend(check_p61970_456_statevariables_ap_con_complex_explicit_crossprofile_shacl_sv_switch_sv_switch_sw_etc(dataset));
    v.extend(check_p61970_456_statevariables_ap_con_complex_explicit_crossprofile_shacl_sv_tap_step_sv_tap_ste_etc(dataset));
    v.extend(check_p61970_456_statevariables_ap_con_complex_explicit_crossprofile_shacl_sv_voltage_sv_voltage_etc(dataset));
    v.extend(check_p61970_456_statevariables_ap_con_complex_explicit_crossprofile_shacl_topological_island_top_etc(dataset));
    v.extend(check_p61970_456_statevariables_ap_con_complex_explicit_crossprofile_shacl_topological_island_top_etc_2(dataset));
    v
}

pub fn check_p61970_456_statevariables_ap_con_complex_explicit_crossprofile_shacl_dc_topological_island_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("DCTopologicalIsland").into_iter().flatten() {
        let entry = &dataset.entries[mrid];
        let obj = match entry.element.as_any()
            .downcast_ref::<cimstructs::DCTopologicalIsland>() {
            Some(o) => o, None => continue,
        };
        for r in &obj.dc_topological_nodes {
            let ref_id = r.mrid.trim_start_matches('#');
            if ref_id.is_empty() { continue; }
            if let Some(ref_entry) = dataset.entries.get(ref_id) {
                let allowed: &[&str] = &["DCTopologicalNode"];
                if !allowed.contains(&ref_entry.element.type_name()) {
                    violations.push(Violation {
                        object_id:   mrid.clone(),
                        rule_id:     "sv456cpe:DCTopologicalIsland.DCTopologicalNodes-valueType".to_string(),
                        class:       "DCTopologicalIsland".to_string(),
                        property:    "DCTopologicalIsland.DCTopologicalNodes".to_string(),
                        message:     "One of the following does not conform: 1) The value type shall be IRI; 2) The value type is not an instance of the class DCTopologicalNode or its subclass.".to_string(),
                        severity:    "sh:Violation".to_string(),
                        name:        "DCTopologicalIsland.DCTopologicalNodes-valueType".to_string(),
                        description: "This constraint validates the value type of the association at the used direction.".to_string(),
                    });
                }
            }
        }
    }
    violations
}

pub fn check_p61970_456_statevariables_ap_con_complex_explicit_crossprofile_shacl_sv_injection_sv_inject_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("SvInjection").into_iter().flatten() {
        let entry = &dataset.entries[mrid];
        let obj = match entry.element.as_any()
            .downcast_ref::<cimstructs::SvInjection>() {
            Some(o) => o, None => continue,
        };
        let ref_mrid = match obj.topological_node.as_ref() {
            Some(r) => r.mrid.trim_start_matches('#'), None => continue,
        };
        if let Some(ref_entry) = dataset.entries.get(ref_mrid) {
            let allowed: &[&str] = &["TopologicalNode"];
            if !allowed.contains(&ref_entry.element.type_name()) {
                violations.push(Violation {
                    object_id:   mrid.clone(),
                    rule_id:     "sv456cpe:SvInjection.TopologicalNode-valueType".to_string(),
                    class:       "SvInjection".to_string(),
                    property:    "SvInjection.TopologicalNode".to_string(),
                    message:     "One of the following does not conform: 1) The value type shall be IRI; 2) The value type is not an instance of the class TopologicalNode or its subclass.".to_string(),
                    severity:    "sh:Violation".to_string(),
                    name:        "SvInjection.TopologicalNode-valueType".to_string(),
                    description: "This constraint validates the value type of the association at the used direction.".to_string(),
                });
            }
        }
    }
    violations
}

pub fn check_p61970_456_statevariables_ap_con_complex_explicit_crossprofile_shacl_sv_power_flow_sv_power_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("SvPowerFlow").into_iter().flatten() {
        let entry = &dataset.entries[mrid];
        let obj = match entry.element.as_any()
            .downcast_ref::<cimstructs::SvPowerFlow>() {
            Some(o) => o, None => continue,
        };
        let ref_mrid = match obj.terminal.as_ref() {
            Some(r) => r.mrid.trim_start_matches('#'), None => continue,
        };
        if let Some(ref_entry) = dataset.entries.get(ref_mrid) {
            let allowed: &[&str] = &["Terminal"];
            if !allowed.contains(&ref_entry.element.type_name()) {
                violations.push(Violation {
                    object_id:   mrid.clone(),
                    rule_id:     "sv456cpe:SvPowerFlow.Terminal-valueType".to_string(),
                    class:       "SvPowerFlow".to_string(),
                    property:    "SvPowerFlow.Terminal".to_string(),
                    message:     "One of the following does not conform: 1) The value type shall be IRI; 2) The value type is not an instance of the class Terminal or its subclass.".to_string(),
                    severity:    "sh:Violation".to_string(),
                    name:        "SvPowerFlow.Terminal-valueType".to_string(),
                    description: "This constraint validates the value type of the association at the used direction.".to_string(),
                });
            }
        }
    }
    violations
}

pub fn check_p61970_456_statevariables_ap_con_complex_explicit_crossprofile_shacl_sv_shunt_compensator_s_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("SvShuntCompensatorSections").into_iter().flatten() {
        let entry = &dataset.entries[mrid];
        let obj = match entry.element.as_any()
            .downcast_ref::<cimstructs::SvShuntCompensatorSections>() {
            Some(o) => o, None => continue,
        };
        let ref_mrid = match obj.shunt_compensator.as_ref() {
            Some(r) => r.mrid.trim_start_matches('#'), None => continue,
        };
        if let Some(ref_entry) = dataset.entries.get(ref_mrid) {
            let allowed: &[&str] = &["LinearShuntCompensator", "NonlinearShuntCompensator"];
            if !allowed.contains(&ref_entry.element.type_name()) {
                violations.push(Violation {
                    object_id:   mrid.clone(),
                    rule_id:     "sv456cpe:SvShuntCompensatorSections.ShuntCompensator-valueType".to_string(),
                    class:       "SvShuntCompensatorSections".to_string(),
                    property:    "SvShuntCompensatorSections.ShuntCompensator".to_string(),
                    message:     "One of the following does not conform: 1) The value type shall be IRI; 2) The value type is not an instance of the class ShuntCompensator or its subclass.".to_string(),
                    severity:    "sh:Violation".to_string(),
                    name:        "SvShuntCompensatorSections.ShuntCompensator-valueType".to_string(),
                    description: "This constraint validates the value type of the association at the used direction.".to_string(),
                });
            }
        }
    }
    violations
}

pub fn check_p61970_456_statevariables_ap_con_complex_explicit_crossprofile_shacl_sv_status_sv_status_co_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("SvStatus").into_iter().flatten() {
        let entry = &dataset.entries[mrid];
        let obj = match entry.element.as_any()
            .downcast_ref::<cimstructs::SvStatus>() {
            Some(o) => o, None => continue,
        };
        let ref_mrid = match obj.conducting_equipment.as_ref() {
            Some(r) => r.mrid.trim_start_matches('#'), None => continue,
        };
        if let Some(ref_entry) = dataset.entries.get(ref_mrid) {
            let allowed: &[&str] = &["NonConformLoad", "Jumper", "EquivalentShunt", "Fuse", "Cut", "Junction", "PowerElectronicsConnection", "CsConverter", "EnergyConsumer", "AsynchronousMachine", "SynchronousMachine", "EquivalentBranch", "Clamp", "DisconnectingCircuitBreaker", "EquivalentInjection", "StationSupply", "BusbarSection", "ACLineSegment", "StaticVarCompensator", "Disconnector", "GroundingImpedance", "LoadBreakSwitch", "PowerTransformer", "PetersenCoil", "Switch", "LinearShuntCompensator", "Ground", "SeriesCompensator", "GroundDisconnector", "VsConverter", "Breaker", "EnergySource", "NonlinearShuntCompensator", "ExternalNetworkInjection", "ConformLoad", "Equipment"];
            if !allowed.contains(&ref_entry.element.type_name()) {
                violations.push(Violation {
                    object_id:   mrid.clone(),
                    rule_id:     "sv456cpe:SvStatus.ConductingEquipment-valueType".to_string(),
                    class:       "SvStatus".to_string(),
                    property:    "SvStatus.ConductingEquipment".to_string(),
                    message:     "One of the following does not conform: 1) The value type shall be IRI; 2) The value type is not an instance of the class ConductingEquipment or its subclass.".to_string(),
                    severity:    "sh:Violation".to_string(),
                    name:        "SvStatus.ConductingEquipment-valueType".to_string(),
                    description: "This constraint validates the value type of the association at the used direction.".to_string(),
                });
            }
        }
    }
    violations
}

pub fn check_p61970_456_statevariables_ap_con_complex_explicit_crossprofile_shacl_sv_switch_sv_switch_sw_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("SvSwitch").into_iter().flatten() {
        let entry = &dataset.entries[mrid];
        let obj = match entry.element.as_any()
            .downcast_ref::<cimstructs::SvSwitch>() {
            Some(o) => o, None => continue,
        };
        let ref_mrid = match obj.switch.as_ref() {
            Some(r) => r.mrid.trim_start_matches('#'), None => continue,
        };
        if let Some(ref_entry) = dataset.entries.get(ref_mrid) {
            let allowed: &[&str] = &["Switch", "Disconnector", "Jumper", "DisconnectingCircuitBreaker", "Breaker", "LoadBreakSwitch", "Cut", "Fuse", "GroundDisconnector", "Equipment"];
            if !allowed.contains(&ref_entry.element.type_name()) {
                violations.push(Violation {
                    object_id:   mrid.clone(),
                    rule_id:     "sv456cpe:SvSwitch.Switch-valueType".to_string(),
                    class:       "SvSwitch".to_string(),
                    property:    "SvSwitch.Switch".to_string(),
                    message:     "One of the following does not conform: 1) The value type shall be IRI; 2) The value type is not an instance of the class Switch or its subclass.".to_string(),
                    severity:    "sh:Violation".to_string(),
                    name:        "SvSwitch.Switch-valueType".to_string(),
                    description: "This constraint validates the value type of the association at the used direction.".to_string(),
                });
            }
        }
    }
    violations
}

pub fn check_p61970_456_statevariables_ap_con_complex_explicit_crossprofile_shacl_sv_tap_step_sv_tap_ste_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("SvTapStep").into_iter().flatten() {
        let entry = &dataset.entries[mrid];
        let obj = match entry.element.as_any()
            .downcast_ref::<cimstructs::SvTapStep>() {
            Some(o) => o, None => continue,
        };
        let ref_mrid = match obj.tap_changer.as_ref() {
            Some(r) => r.mrid.trim_start_matches('#'), None => continue,
        };
        if let Some(ref_entry) = dataset.entries.get(ref_mrid) {
            let allowed: &[&str] = &["RatioTapChanger", "PhaseTapChangerLinear", "PhaseTapChangerTabular", "PhaseTapChangerAsymmetrical", "PhaseTapChangerSymmetrical"];
            if !allowed.contains(&ref_entry.element.type_name()) {
                violations.push(Violation {
                    object_id:   mrid.clone(),
                    rule_id:     "sv456cpe:SvTapStep.TapChanger-valueType".to_string(),
                    class:       "SvTapStep".to_string(),
                    property:    "SvTapStep.TapChanger".to_string(),
                    message:     "One of the following does not conform: 1) The value type shall be IRI; 2) The value type is not an instance of the class TapChanger or its subclass.".to_string(),
                    severity:    "sh:Violation".to_string(),
                    name:        "SvTapStep.TapChanger-valueType".to_string(),
                    description: "This constraint validates the value type of the association at the used direction.".to_string(),
                });
            }
        }
    }
    violations
}

pub fn check_p61970_456_statevariables_ap_con_complex_explicit_crossprofile_shacl_sv_voltage_sv_voltage_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("SvVoltage").into_iter().flatten() {
        let entry = &dataset.entries[mrid];
        let obj = match entry.element.as_any()
            .downcast_ref::<cimstructs::SvVoltage>() {
            Some(o) => o, None => continue,
        };
        let ref_mrid = match obj.topological_node.as_ref() {
            Some(r) => r.mrid.trim_start_matches('#'), None => continue,
        };
        if let Some(ref_entry) = dataset.entries.get(ref_mrid) {
            let allowed: &[&str] = &["TopologicalNode"];
            if !allowed.contains(&ref_entry.element.type_name()) {
                violations.push(Violation {
                    object_id:   mrid.clone(),
                    rule_id:     "sv456cpe:SvVoltage.TopologicalNode-valueType".to_string(),
                    class:       "SvVoltage".to_string(),
                    property:    "SvVoltage.TopologicalNode".to_string(),
                    message:     "One of the following does not conform: 1) The value type shall be IRI; 2) The value type is not an instance of the class TopologicalNode or its subclass.".to_string(),
                    severity:    "sh:Violation".to_string(),
                    name:        "SvVoltage.TopologicalNode-valueType".to_string(),
                    description: "This constraint validates the value type of the association at the used direction.".to_string(),
                });
            }
        }
    }
    violations
}

pub fn check_p61970_456_statevariables_ap_con_complex_explicit_crossprofile_shacl_topological_island_top_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("TopologicalIsland").into_iter().flatten() {
        let entry = &dataset.entries[mrid];
        let obj = match entry.element.as_any()
            .downcast_ref::<cimstructs::TopologicalIsland>() {
            Some(o) => o, None => continue,
        };
        let ref_mrid = match obj.angle_ref_topological_node.as_ref() {
            Some(r) => r.mrid.trim_start_matches('#'), None => continue,
        };
        if let Some(ref_entry) = dataset.entries.get(ref_mrid) {
            let allowed: &[&str] = &["TopologicalNode"];
            if !allowed.contains(&ref_entry.element.type_name()) {
                violations.push(Violation {
                    object_id:   mrid.clone(),
                    rule_id:     "sv456cpe:TopologicalIsland.AngleRefTopologicalNode-valueType".to_string(),
                    class:       "TopologicalIsland".to_string(),
                    property:    "TopologicalIsland.AngleRefTopologicalNode".to_string(),
                    message:     "One of the following does not conform: 1) The value type shall be IRI; 2) The value type is not an instance of the class TopologicalNode or its subclass.".to_string(),
                    severity:    "sh:Violation".to_string(),
                    name:        "TopologicalIsland.AngleRefTopologicalNode-valueType".to_string(),
                    description: "This constraint validates the value type of the association at the used direction.".to_string(),
                });
            }
        }
    }
    violations
}

pub fn check_p61970_456_statevariables_ap_con_complex_explicit_crossprofile_shacl_topological_island_top_etc_2(dataset: &CimDataset) -> Vec<Violation> {
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("TopologicalIsland").into_iter().flatten() {
        let entry = &dataset.entries[mrid];
        let obj = match entry.element.as_any()
            .downcast_ref::<cimstructs::TopologicalIsland>() {
            Some(o) => o, None => continue,
        };
        for r in &obj.topological_nodes {
            let ref_id = r.mrid.trim_start_matches('#');
            if ref_id.is_empty() { continue; }
            if let Some(ref_entry) = dataset.entries.get(ref_id) {
                let allowed: &[&str] = &["TopologicalNode"];
                if !allowed.contains(&ref_entry.element.type_name()) {
                    violations.push(Violation {
                        object_id:   mrid.clone(),
                        rule_id:     "sv456cpe:TopologicalIsland.TopologicalNodes-valueType".to_string(),
                        class:       "TopologicalIsland".to_string(),
                        property:    "TopologicalIsland.TopologicalNodes".to_string(),
                        message:     "One of the following does not conform: 1) The value type shall be IRI; 2) The value type is not an instance of the class TopologicalNode or its subclass.".to_string(),
                        severity:    "sh:Violation".to_string(),
                        name:        "TopologicalIsland.TopologicalNodes-valueType".to_string(),
                        description: "This constraint validates the value type of the association at the used direction.".to_string(),
                    });
                }
            }
        }
    }
    violations
}

