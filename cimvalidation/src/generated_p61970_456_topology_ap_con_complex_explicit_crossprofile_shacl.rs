// Generated by cimgen — do not edit by hand.
#![allow(clippy::all, dead_code, unused)]
use cimdecoder::CimDataset;
use crate::Violation;
use crate::helpers::*;

pub fn validate_p61970_456_topology_ap_con_complex_explicit_crossprofile_shacl(dataset: &CimDataset) -> Vec<Violation> {
    let mut v = Vec::new();
    v.extend(check_p61970_456_topology_ap_con_complex_explicit_crossprofile_shacl_dc_topological_node_dc_topol_etc(dataset));
    v.extend(check_p61970_456_topology_ap_con_complex_explicit_crossprofile_shacl_topological_node_topological_etc(dataset));
    v.extend(check_p61970_456_topology_ap_con_complex_explicit_crossprofile_shacl_topological_node_topological_etc_2(dataset));
    v
}

pub fn check_p61970_456_topology_ap_con_complex_explicit_crossprofile_shacl_dc_topological_node_dc_topol_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("DCTopologicalNode").into_iter().flatten() {
        let entry = &dataset.entries[mrid];
        let obj = match entry.element.as_any()
            .downcast_ref::<cimstructs::DCTopologicalNode>() {
            Some(o) => o, None => continue,
        };
        let ref_mrid = match obj.dc_equipment_container.as_ref() {
            Some(r) => r.mrid.trim_start_matches('#'), None => continue,
        };
        if let Some(ref_entry) = dataset.entries.get(ref_mrid) {
            let allowed: &[&str] = &["DCLine", "DCConverterUnit"];
            if !allowed.contains(&ref_entry.element.type_name()) {
                violations.push(Violation {
                    object_id:   mrid.clone(),
                    rule_id:     "tp456cpe:DCTopologicalNode.DCEquipmentContainer-valueType".to_string(),
                    class:       "DCTopologicalNode".to_string(),
                    property:    "DCTopologicalNode.DCEquipmentContainer".to_string(),
                    message:     "One of the following does not conform: 1) The value type shall be IRI; 2) The value type is not an instance of the class DCEquipmentContainer or its subclass.".to_string(),
                    severity:    "sh:Violation".to_string(),
                    name:        "DCTopologicalNode.DCEquipmentContainer-valueType".to_string(),
                    description: "This constraint validates the value type of the association at the used direction.".to_string(),
                });
            }
        }
    }
    violations
}

pub fn check_p61970_456_topology_ap_con_complex_explicit_crossprofile_shacl_topological_node_topological_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("TopologicalNode").into_iter().flatten() {
        let entry = &dataset.entries[mrid];
        let obj = match entry.element.as_any()
            .downcast_ref::<cimstructs::TopologicalNode>() {
            Some(o) => o, None => continue,
        };
        let ref_mrid = match obj.base_voltage.as_ref() {
            Some(r) => r.mrid.trim_start_matches('#'), None => continue,
        };
        if let Some(ref_entry) = dataset.entries.get(ref_mrid) {
            let allowed: &[&str] = &["BaseVoltage"];
            if !allowed.contains(&ref_entry.element.type_name()) {
                violations.push(Violation {
                    object_id:   mrid.clone(),
                    rule_id:     "tp456cpe:TopologicalNode.BaseVoltage-valueType".to_string(),
                    class:       "TopologicalNode".to_string(),
                    property:    "TopologicalNode.BaseVoltage".to_string(),
                    message:     "One of the following does not conform: 1) The value type shall be IRI; 2) The value type is not an instance of the class BaseVoltage or its subclass.".to_string(),
                    severity:    "sh:Violation".to_string(),
                    name:        "TopologicalNode.BaseVoltage-valueType".to_string(),
                    description: "This constraint validates the value type of the association at the used direction.".to_string(),
                });
            }
        }
    }
    violations
}

pub fn check_p61970_456_topology_ap_con_complex_explicit_crossprofile_shacl_topological_node_topological_etc_2(dataset: &CimDataset) -> Vec<Violation> {
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("TopologicalNode").into_iter().flatten() {
        let entry = &dataset.entries[mrid];
        let obj = match entry.element.as_any()
            .downcast_ref::<cimstructs::TopologicalNode>() {
            Some(o) => o, None => continue,
        };
        let ref_mrid = match obj.connectivity_node_container.as_ref() {
            Some(r) => r.mrid.trim_start_matches('#'), None => continue,
        };
        if let Some(ref_entry) = dataset.entries.get(ref_mrid) {
            let allowed: &[&str] = &["EquivalentNetwork", "DCLine", "DCConverterUnit", "Bay", "Line", "Substation", "VoltageLevel"];
            if !allowed.contains(&ref_entry.element.type_name()) {
                violations.push(Violation {
                    object_id:   mrid.clone(),
                    rule_id:     "tp456cpe:TopologicalNode.ConnectivityNodeContainer-valueType".to_string(),
                    class:       "TopologicalNode".to_string(),
                    property:    "TopologicalNode.ConnectivityNodeContainer".to_string(),
                    message:     "One of the following does not conform: 1) The value type shall be IRI; 2) The value type is not an instance of the class ConnectivityNodeContainer or its subclass.".to_string(),
                    severity:    "sh:Violation".to_string(),
                    name:        "TopologicalNode.ConnectivityNodeContainer-valueType".to_string(),
                    description: "This constraint validates the value type of the association at the used direction.".to_string(),
                });
            }
        }
    }
    violations
}

