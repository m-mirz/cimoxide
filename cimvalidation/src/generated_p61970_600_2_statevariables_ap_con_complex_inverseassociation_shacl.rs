// Generated by cimgen — do not edit by hand.
#![allow(clippy::all, dead_code, unused)]
use cimdecoder::CimDataset;
use crate::Violation;
use crate::helpers::*;

pub fn validate_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl(dataset: &CimDataset) -> Vec<Violation> {
    let mut v = Vec::new();
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_cs_converter_sv_status_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_vs_converter_sv_status_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_clamp_sv_status_conduct_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_ac_line_segment_sv_stat_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_busbar_section_sv_statu_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_junction_sv_status_cond_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_grounding_impedance_sv_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_petersen_coil_sv_status_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_energy_source_sv_status_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_energy_consumer_sv_stat_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_conform_load_sv_status_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_non_conform_load_sv_sta_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_station_supply_sv_statu_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_linear_shunt_compensato_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_nonlinear_shunt_compens_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_external_network_inject_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_power_electronics_conne_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_static_var_compensator_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_synchronous_machine_sv_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_asynchronous_machine_sv_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_equivalent_branch_sv_st_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_equivalent_injection_sv_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_equivalent_shunt_sv_sta_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_ground_sv_status_conduc_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_power_transformer_sv_st_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_series_compensator_sv_s_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_switch_sv_status_conduc_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_fuse_sv_status_conducti_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_ground_disconnector_sv_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_jumper_sv_status_conduc_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_breaker_sv_status_condu_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_disconnecting_circuit_b_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_load_break_switch_sv_st_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_disconnector_sv_status_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_cut_sv_status_conductin_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_dc_topological_node_dc_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_linear_shunt_compensato_etc_2(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_nonlinear_shunt_compens_etc_2(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_ratio_tap_changer_sv_ta_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_phase_tap_changer_symme_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_phase_tap_changer_asymm_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_phase_tap_changer_linea_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_phase_tap_changer_tabul_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_terminal_sv_power_flow_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_topological_node_sv_inj_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_topological_node_topolo_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_topological_node_sv_vol_etc(dataset));
    v.extend(check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_topological_node_topolo_etc_2(dataset));
    v
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_cs_converter_sv_status_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvStatus").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvStatus>()) {
            if let Some(r) = &src.conducting_equipment { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("CsConverter").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:ConductingEquipment.SvStatus-cardinality".to_string(),
                class:       "CsConverter".to_string(),
                property:    "ConductingEquipment.SvStatus-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "ConductingEquipment.SvStatus-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_vs_converter_sv_status_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvStatus").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvStatus>()) {
            if let Some(r) = &src.conducting_equipment { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("VsConverter").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:ConductingEquipment.SvStatus-cardinality".to_string(),
                class:       "VsConverter".to_string(),
                property:    "ConductingEquipment.SvStatus-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "ConductingEquipment.SvStatus-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_clamp_sv_status_conduct_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvStatus").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvStatus>()) {
            if let Some(r) = &src.conducting_equipment { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("Clamp").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:ConductingEquipment.SvStatus-cardinality".to_string(),
                class:       "Clamp".to_string(),
                property:    "ConductingEquipment.SvStatus-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "ConductingEquipment.SvStatus-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_ac_line_segment_sv_stat_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvStatus").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvStatus>()) {
            if let Some(r) = &src.conducting_equipment { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("ACLineSegment").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:ConductingEquipment.SvStatus-cardinality".to_string(),
                class:       "ACLineSegment".to_string(),
                property:    "ConductingEquipment.SvStatus-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "ConductingEquipment.SvStatus-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_busbar_section_sv_statu_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvStatus").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvStatus>()) {
            if let Some(r) = &src.conducting_equipment { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("BusbarSection").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:ConductingEquipment.SvStatus-cardinality".to_string(),
                class:       "BusbarSection".to_string(),
                property:    "ConductingEquipment.SvStatus-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "ConductingEquipment.SvStatus-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_junction_sv_status_cond_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvStatus").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvStatus>()) {
            if let Some(r) = &src.conducting_equipment { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("Junction").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:ConductingEquipment.SvStatus-cardinality".to_string(),
                class:       "Junction".to_string(),
                property:    "ConductingEquipment.SvStatus-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "ConductingEquipment.SvStatus-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_grounding_impedance_sv_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvStatus").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvStatus>()) {
            if let Some(r) = &src.conducting_equipment { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("GroundingImpedance").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:ConductingEquipment.SvStatus-cardinality".to_string(),
                class:       "GroundingImpedance".to_string(),
                property:    "ConductingEquipment.SvStatus-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "ConductingEquipment.SvStatus-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_petersen_coil_sv_status_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvStatus").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvStatus>()) {
            if let Some(r) = &src.conducting_equipment { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("PetersenCoil").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:ConductingEquipment.SvStatus-cardinality".to_string(),
                class:       "PetersenCoil".to_string(),
                property:    "ConductingEquipment.SvStatus-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "ConductingEquipment.SvStatus-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_energy_source_sv_status_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvStatus").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvStatus>()) {
            if let Some(r) = &src.conducting_equipment { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("EnergySource").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:ConductingEquipment.SvStatus-cardinality".to_string(),
                class:       "EnergySource".to_string(),
                property:    "ConductingEquipment.SvStatus-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "ConductingEquipment.SvStatus-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_energy_consumer_sv_stat_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvStatus").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvStatus>()) {
            if let Some(r) = &src.conducting_equipment { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("EnergyConsumer").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:ConductingEquipment.SvStatus-cardinality".to_string(),
                class:       "EnergyConsumer".to_string(),
                property:    "ConductingEquipment.SvStatus-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "ConductingEquipment.SvStatus-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_conform_load_sv_status_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvStatus").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvStatus>()) {
            if let Some(r) = &src.conducting_equipment { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("ConformLoad").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:ConductingEquipment.SvStatus-cardinality".to_string(),
                class:       "ConformLoad".to_string(),
                property:    "ConductingEquipment.SvStatus-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "ConductingEquipment.SvStatus-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_non_conform_load_sv_sta_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvStatus").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvStatus>()) {
            if let Some(r) = &src.conducting_equipment { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("NonConformLoad").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:ConductingEquipment.SvStatus-cardinality".to_string(),
                class:       "NonConformLoad".to_string(),
                property:    "ConductingEquipment.SvStatus-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "ConductingEquipment.SvStatus-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_station_supply_sv_statu_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvStatus").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvStatus>()) {
            if let Some(r) = &src.conducting_equipment { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("StationSupply").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:ConductingEquipment.SvStatus-cardinality".to_string(),
                class:       "StationSupply".to_string(),
                property:    "ConductingEquipment.SvStatus-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "ConductingEquipment.SvStatus-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_linear_shunt_compensato_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvStatus").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvStatus>()) {
            if let Some(r) = &src.conducting_equipment { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("LinearShuntCompensator").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:ConductingEquipment.SvStatus-cardinality".to_string(),
                class:       "LinearShuntCompensator".to_string(),
                property:    "ConductingEquipment.SvStatus-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "ConductingEquipment.SvStatus-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_nonlinear_shunt_compens_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvStatus").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvStatus>()) {
            if let Some(r) = &src.conducting_equipment { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("NonlinearShuntCompensator").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:ConductingEquipment.SvStatus-cardinality".to_string(),
                class:       "NonlinearShuntCompensator".to_string(),
                property:    "ConductingEquipment.SvStatus-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "ConductingEquipment.SvStatus-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_external_network_inject_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvStatus").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvStatus>()) {
            if let Some(r) = &src.conducting_equipment { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("ExternalNetworkInjection").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:ConductingEquipment.SvStatus-cardinality".to_string(),
                class:       "ExternalNetworkInjection".to_string(),
                property:    "ConductingEquipment.SvStatus-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "ConductingEquipment.SvStatus-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_power_electronics_conne_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvStatus").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvStatus>()) {
            if let Some(r) = &src.conducting_equipment { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("PowerElectronicsConnection").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:ConductingEquipment.SvStatus-cardinality".to_string(),
                class:       "PowerElectronicsConnection".to_string(),
                property:    "ConductingEquipment.SvStatus-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "ConductingEquipment.SvStatus-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_static_var_compensator_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvStatus").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvStatus>()) {
            if let Some(r) = &src.conducting_equipment { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("StaticVarCompensator").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:ConductingEquipment.SvStatus-cardinality".to_string(),
                class:       "StaticVarCompensator".to_string(),
                property:    "ConductingEquipment.SvStatus-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "ConductingEquipment.SvStatus-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_synchronous_machine_sv_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvStatus").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvStatus>()) {
            if let Some(r) = &src.conducting_equipment { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("SynchronousMachine").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:ConductingEquipment.SvStatus-cardinality".to_string(),
                class:       "SynchronousMachine".to_string(),
                property:    "ConductingEquipment.SvStatus-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "ConductingEquipment.SvStatus-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_asynchronous_machine_sv_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvStatus").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvStatus>()) {
            if let Some(r) = &src.conducting_equipment { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("AsynchronousMachine").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:ConductingEquipment.SvStatus-cardinality".to_string(),
                class:       "AsynchronousMachine".to_string(),
                property:    "ConductingEquipment.SvStatus-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "ConductingEquipment.SvStatus-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_equivalent_branch_sv_st_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvStatus").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvStatus>()) {
            if let Some(r) = &src.conducting_equipment { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("EquivalentBranch").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:ConductingEquipment.SvStatus-cardinality".to_string(),
                class:       "EquivalentBranch".to_string(),
                property:    "ConductingEquipment.SvStatus-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "ConductingEquipment.SvStatus-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_equivalent_injection_sv_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvStatus").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvStatus>()) {
            if let Some(r) = &src.conducting_equipment { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("EquivalentInjection").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:ConductingEquipment.SvStatus-cardinality".to_string(),
                class:       "EquivalentInjection".to_string(),
                property:    "ConductingEquipment.SvStatus-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "ConductingEquipment.SvStatus-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_equivalent_shunt_sv_sta_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvStatus").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvStatus>()) {
            if let Some(r) = &src.conducting_equipment { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("EquivalentShunt").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:ConductingEquipment.SvStatus-cardinality".to_string(),
                class:       "EquivalentShunt".to_string(),
                property:    "ConductingEquipment.SvStatus-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "ConductingEquipment.SvStatus-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_ground_sv_status_conduc_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvStatus").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvStatus>()) {
            if let Some(r) = &src.conducting_equipment { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("Ground").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:ConductingEquipment.SvStatus-cardinality".to_string(),
                class:       "Ground".to_string(),
                property:    "ConductingEquipment.SvStatus-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "ConductingEquipment.SvStatus-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_power_transformer_sv_st_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvStatus").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvStatus>()) {
            if let Some(r) = &src.conducting_equipment { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("PowerTransformer").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:ConductingEquipment.SvStatus-cardinality".to_string(),
                class:       "PowerTransformer".to_string(),
                property:    "ConductingEquipment.SvStatus-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "ConductingEquipment.SvStatus-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_series_compensator_sv_s_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvStatus").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvStatus>()) {
            if let Some(r) = &src.conducting_equipment { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("SeriesCompensator").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:ConductingEquipment.SvStatus-cardinality".to_string(),
                class:       "SeriesCompensator".to_string(),
                property:    "ConductingEquipment.SvStatus-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "ConductingEquipment.SvStatus-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_switch_sv_status_conduc_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvStatus").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvStatus>()) {
            if let Some(r) = &src.conducting_equipment { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("Switch").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:ConductingEquipment.SvStatus-cardinality".to_string(),
                class:       "Switch".to_string(),
                property:    "ConductingEquipment.SvStatus-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "ConductingEquipment.SvStatus-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_fuse_sv_status_conducti_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvStatus").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvStatus>()) {
            if let Some(r) = &src.conducting_equipment { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("Fuse").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:ConductingEquipment.SvStatus-cardinality".to_string(),
                class:       "Fuse".to_string(),
                property:    "ConductingEquipment.SvStatus-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "ConductingEquipment.SvStatus-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_ground_disconnector_sv_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvStatus").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvStatus>()) {
            if let Some(r) = &src.conducting_equipment { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("GroundDisconnector").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:ConductingEquipment.SvStatus-cardinality".to_string(),
                class:       "GroundDisconnector".to_string(),
                property:    "ConductingEquipment.SvStatus-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "ConductingEquipment.SvStatus-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_jumper_sv_status_conduc_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvStatus").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvStatus>()) {
            if let Some(r) = &src.conducting_equipment { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("Jumper").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:ConductingEquipment.SvStatus-cardinality".to_string(),
                class:       "Jumper".to_string(),
                property:    "ConductingEquipment.SvStatus-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "ConductingEquipment.SvStatus-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_breaker_sv_status_condu_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvStatus").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvStatus>()) {
            if let Some(r) = &src.conducting_equipment { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("Breaker").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:ConductingEquipment.SvStatus-cardinality".to_string(),
                class:       "Breaker".to_string(),
                property:    "ConductingEquipment.SvStatus-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "ConductingEquipment.SvStatus-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_disconnecting_circuit_b_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvStatus").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvStatus>()) {
            if let Some(r) = &src.conducting_equipment { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("DisconnectingCircuitBreaker").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:ConductingEquipment.SvStatus-cardinality".to_string(),
                class:       "DisconnectingCircuitBreaker".to_string(),
                property:    "ConductingEquipment.SvStatus-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "ConductingEquipment.SvStatus-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_load_break_switch_sv_st_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvStatus").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvStatus>()) {
            if let Some(r) = &src.conducting_equipment { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("LoadBreakSwitch").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:ConductingEquipment.SvStatus-cardinality".to_string(),
                class:       "LoadBreakSwitch".to_string(),
                property:    "ConductingEquipment.SvStatus-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "ConductingEquipment.SvStatus-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_disconnector_sv_status_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvStatus").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvStatus>()) {
            if let Some(r) = &src.conducting_equipment { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("Disconnector").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:ConductingEquipment.SvStatus-cardinality".to_string(),
                class:       "Disconnector".to_string(),
                property:    "ConductingEquipment.SvStatus-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "ConductingEquipment.SvStatus-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_cut_sv_status_conductin_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvStatus").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvStatus>()) {
            if let Some(r) = &src.conducting_equipment { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("Cut").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:ConductingEquipment.SvStatus-cardinality".to_string(),
                class:       "Cut".to_string(),
                property:    "ConductingEquipment.SvStatus-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "ConductingEquipment.SvStatus-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_dc_topological_node_dc_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("DCTopologicalIsland").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::DCTopologicalIsland>()) {
            for r in &src.dc_topological_nodes { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("DCTopologicalNode").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:DCTopologicalNode.DCTopologicalIsland-cardinality".to_string(),
                class:       "DCTopologicalNode".to_string(),
                property:    "DCTopologicalNode.DCTopologicalIsland-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "DCTopologicalNode.DCTopologicalIsland-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_linear_shunt_compensato_etc_2(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvShuntCompensatorSections").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvShuntCompensatorSections>()) {
            if let Some(r) = &src.shunt_compensator { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("LinearShuntCompensator").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:ShuntCompensator.SvShuntCompensatorSections-cardinality".to_string(),
                class:       "LinearShuntCompensator".to_string(),
                property:    "ShuntCompensator.SvShuntCompensatorSections-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "ShuntCompensator.SvShuntCompensatorSections-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_nonlinear_shunt_compens_etc_2(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvShuntCompensatorSections").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvShuntCompensatorSections>()) {
            if let Some(r) = &src.shunt_compensator { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("NonlinearShuntCompensator").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:ShuntCompensator.SvShuntCompensatorSections-cardinality".to_string(),
                class:       "NonlinearShuntCompensator".to_string(),
                property:    "ShuntCompensator.SvShuntCompensatorSections-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "ShuntCompensator.SvShuntCompensatorSections-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_ratio_tap_changer_sv_ta_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvTapStep").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvTapStep>()) {
            if let Some(r) = &src.tap_changer { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("RatioTapChanger").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:TapChanger.SvTapStep-cardinality".to_string(),
                class:       "RatioTapChanger".to_string(),
                property:    "TapChanger.SvTapStep-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "TapChanger.SvTapStep-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_phase_tap_changer_symme_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvTapStep").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvTapStep>()) {
            if let Some(r) = &src.tap_changer { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("PhaseTapChangerSymmetrical").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:TapChanger.SvTapStep-cardinality".to_string(),
                class:       "PhaseTapChangerSymmetrical".to_string(),
                property:    "TapChanger.SvTapStep-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "TapChanger.SvTapStep-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_phase_tap_changer_asymm_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvTapStep").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvTapStep>()) {
            if let Some(r) = &src.tap_changer { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("PhaseTapChangerAsymmetrical").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:TapChanger.SvTapStep-cardinality".to_string(),
                class:       "PhaseTapChangerAsymmetrical".to_string(),
                property:    "TapChanger.SvTapStep-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "TapChanger.SvTapStep-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_phase_tap_changer_linea_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvTapStep").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvTapStep>()) {
            if let Some(r) = &src.tap_changer { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("PhaseTapChangerLinear").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:TapChanger.SvTapStep-cardinality".to_string(),
                class:       "PhaseTapChangerLinear".to_string(),
                property:    "TapChanger.SvTapStep-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "TapChanger.SvTapStep-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_phase_tap_changer_tabul_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvTapStep").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvTapStep>()) {
            if let Some(r) = &src.tap_changer { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("PhaseTapChangerTabular").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:TapChanger.SvTapStep-cardinality".to_string(),
                class:       "PhaseTapChangerTabular".to_string(),
                property:    "TapChanger.SvTapStep-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "TapChanger.SvTapStep-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_terminal_sv_power_flow_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvPowerFlow").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvPowerFlow>()) {
            if let Some(r) = &src.terminal { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("Terminal").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:Terminal.SvPowerFlow-cardinality".to_string(),
                class:       "Terminal".to_string(),
                property:    "Terminal.SvPowerFlow-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "Terminal.SvPowerFlow-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_topological_node_sv_inj_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvInjection").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvInjection>()) {
            if let Some(r) = &src.topological_node { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("TopologicalNode").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:TopologicalNode.SvInjection-cardinality".to_string(),
                class:       "TopologicalNode".to_string(),
                property:    "TopologicalNode.SvInjection-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "TopologicalNode.SvInjection-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_topological_node_topolo_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("TopologicalIsland").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::TopologicalIsland>()) {
            if let Some(r) = &src.angle_ref_topological_node { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("TopologicalNode").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:TopologicalNode.AngleRefTopologicalIsland-cardinality".to_string(),
                class:       "TopologicalNode".to_string(),
                property:    "TopologicalNode.AngleRefTopologicalIsland-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "TopologicalNode.AngleRefTopologicalIsland-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_topological_node_sv_vol_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("SvVoltage").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::SvVoltage>()) {
            if let Some(r) = &src.topological_node { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("TopologicalNode").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:TopologicalNode.SvVoltage-cardinality".to_string(),
                class:       "TopologicalNode".to_string(),
                property:    "TopologicalNode.SvVoltage-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "TopologicalNode.SvVoltage-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_statevariables_ap_con_complex_inverseassociation_shacl_topological_node_topolo_etc_2(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("TopologicalIsland").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::TopologicalIsland>()) {
            for r in &src.topological_nodes { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("TopologicalNode").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "svi:TopologicalNode.TopologicalIsland-cardinality".to_string(),
                class:       "TopologicalNode".to_string(),
                property:    "TopologicalNode.TopologicalIsland-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "TopologicalNode.TopologicalIsland-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

