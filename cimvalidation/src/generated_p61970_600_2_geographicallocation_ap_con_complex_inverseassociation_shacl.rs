// Generated by cimgen — do not edit by hand.
#![allow(clippy::all, dead_code, unused)]
use cimdecoder::CimDataset;
use crate::Violation;
use crate::helpers::*;

pub fn validate_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl(dataset: &CimDataset) -> Vec<Violation> {
    let mut v = Vec::new();
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_boundary_point_lo_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_caes_plant_locati_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_cogeneration_plan_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_combined_cycle_pl_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_control_area_loca_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_hydro_power_plant_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_regulating_contro_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_tap_changer_contr_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_solar_power_plant_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_ratio_tap_changer_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_phase_tap_changer_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_phase_tap_changer_etc_2(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_phase_tap_changer_etc_3(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_wind_power_plant_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_cs_converter_loca_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_vs_converter_loca_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_clamp_location_po_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_ac_line_segment_l_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_busbar_section_lo_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_junction_location_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_grounding_impedan_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_petersen_coil_loc_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_energy_source_loc_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_energy_consumer_l_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_conform_load_loca_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_non_conform_load_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_station_supply_lo_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_linear_shunt_comp_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_nonlinear_shunt_c_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_external_network_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_power_electronics_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_static_var_compen_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_synchronous_machi_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_asynchronous_mach_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_equivalent_branch_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_equivalent_inject_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_equivalent_shunt_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_ground_location_p_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_power_transformer_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_series_compensato_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_switch_location_p_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_fuse_location_pow_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_ground_disconnect_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_jumper_location_p_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_breaker_location_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_disconnecting_cir_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_load_break_switch_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_disconnector_loca_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_cut_location_powe_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_fault_indicator_l_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_surge_arrester_lo_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_wave_trap_locatio_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_current_transform_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_potential_transfo_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_post_line_sensor_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_generating_unit_l_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_hydro_generating_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_nuclear_generatin_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_solar_generating_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_thermal_generatin_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_wind_generating_u_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_hydro_pump_locati_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_power_electronics_etc_2(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_photo_voltaic_uni_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_battery_unit_loca_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_bay_location_powe_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_line_location_pow_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_voltage_level_loc_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_substation_locati_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_equivalent_networ_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_dc_busbar_locatio_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_dc_chopper_locati_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_dc_ground_locatio_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_dc_line_segment_l_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_dc_series_device_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_dc_shunt_location_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_dc_switch_locatio_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_dc_disconnector_l_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_dc_breaker_locati_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_dc_line_location_etc(dataset));
    v.extend(check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_dc_converter_unit_etc(dataset));
    v
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_boundary_point_lo_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("BoundaryPoint").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "BoundaryPoint".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_caes_plant_locati_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("CAESPlant").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "CAESPlant".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_cogeneration_plan_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("CogenerationPlant").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "CogenerationPlant".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_combined_cycle_pl_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("CombinedCyclePlant").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "CombinedCyclePlant".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_control_area_loca_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("ControlArea").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "ControlArea".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_hydro_power_plant_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("HydroPowerPlant").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "HydroPowerPlant".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_regulating_contro_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("RegulatingControl").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "RegulatingControl".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_tap_changer_contr_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("TapChangerControl").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "TapChangerControl".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_solar_power_plant_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("SolarPowerPlant").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "SolarPowerPlant".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_ratio_tap_changer_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("RatioTapChanger").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "RatioTapChanger".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_phase_tap_changer_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("PhaseTapChangerSymmetrical").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "PhaseTapChangerSymmetrical".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_phase_tap_changer_etc_2(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("PhaseTapChangerAsymmetrical").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "PhaseTapChangerAsymmetrical".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_phase_tap_changer_etc_3(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("PhaseTapChangerLinear").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "PhaseTapChangerLinear".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_wind_power_plant_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("WindPowerPlant").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "WindPowerPlant".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_cs_converter_loca_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("CsConverter").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "CsConverter".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_vs_converter_loca_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("VsConverter").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "VsConverter".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_clamp_location_po_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("Clamp").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "Clamp".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_ac_line_segment_l_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("ACLineSegment").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "ACLineSegment".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_busbar_section_lo_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("BusbarSection").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "BusbarSection".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_junction_location_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("Junction").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "Junction".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_grounding_impedan_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("GroundingImpedance").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "GroundingImpedance".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_petersen_coil_loc_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("PetersenCoil").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "PetersenCoil".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_energy_source_loc_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("EnergySource").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "EnergySource".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_energy_consumer_l_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("EnergyConsumer").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "EnergyConsumer".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_conform_load_loca_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("ConformLoad").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "ConformLoad".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_non_conform_load_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("NonConformLoad").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "NonConformLoad".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_station_supply_lo_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("StationSupply").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "StationSupply".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_linear_shunt_comp_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("LinearShuntCompensator").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "LinearShuntCompensator".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_nonlinear_shunt_c_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("NonlinearShuntCompensator").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "NonlinearShuntCompensator".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_external_network_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("ExternalNetworkInjection").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "ExternalNetworkInjection".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_power_electronics_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("PowerElectronicsConnection").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "PowerElectronicsConnection".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_static_var_compen_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("StaticVarCompensator").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "StaticVarCompensator".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_synchronous_machi_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("SynchronousMachine").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "SynchronousMachine".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_asynchronous_mach_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("AsynchronousMachine").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "AsynchronousMachine".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_equivalent_branch_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("EquivalentBranch").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "EquivalentBranch".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_equivalent_inject_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("EquivalentInjection").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "EquivalentInjection".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_equivalent_shunt_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("EquivalentShunt").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "EquivalentShunt".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_ground_location_p_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("Ground").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "Ground".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_power_transformer_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("PowerTransformer").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "PowerTransformer".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_series_compensato_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("SeriesCompensator").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "SeriesCompensator".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_switch_location_p_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("Switch").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "Switch".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_fuse_location_pow_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("Fuse").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "Fuse".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_ground_disconnect_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("GroundDisconnector").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "GroundDisconnector".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_jumper_location_p_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("Jumper").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "Jumper".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_breaker_location_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("Breaker").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "Breaker".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_disconnecting_cir_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("DisconnectingCircuitBreaker").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "DisconnectingCircuitBreaker".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_load_break_switch_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("LoadBreakSwitch").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "LoadBreakSwitch".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_disconnector_loca_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("Disconnector").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "Disconnector".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_cut_location_powe_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("Cut").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "Cut".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_fault_indicator_l_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("FaultIndicator").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "FaultIndicator".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_surge_arrester_lo_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("SurgeArrester").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "SurgeArrester".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_wave_trap_locatio_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("WaveTrap").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "WaveTrap".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_current_transform_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("CurrentTransformer").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "CurrentTransformer".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_potential_transfo_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("PotentialTransformer").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "PotentialTransformer".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_post_line_sensor_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("PostLineSensor").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "PostLineSensor".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_generating_unit_l_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("GeneratingUnit").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "GeneratingUnit".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_hydro_generating_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("HydroGeneratingUnit").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "HydroGeneratingUnit".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_nuclear_generatin_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("NuclearGeneratingUnit").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "NuclearGeneratingUnit".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_solar_generating_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("SolarGeneratingUnit").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "SolarGeneratingUnit".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_thermal_generatin_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("ThermalGeneratingUnit").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "ThermalGeneratingUnit".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_wind_generating_u_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("WindGeneratingUnit").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "WindGeneratingUnit".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_hydro_pump_locati_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("HydroPump").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "HydroPump".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_power_electronics_etc_2(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("PowerElectronicsWindUnit").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "PowerElectronicsWindUnit".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_photo_voltaic_uni_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("PhotoVoltaicUnit").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "PhotoVoltaicUnit".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_battery_unit_loca_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("BatteryUnit").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "BatteryUnit".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_bay_location_powe_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("Bay").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "Bay".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_line_location_pow_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("Line").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "Line".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_voltage_level_loc_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("VoltageLevel").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "VoltageLevel".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_substation_locati_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("Substation").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "Substation".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_equivalent_networ_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("EquivalentNetwork").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "EquivalentNetwork".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_dc_busbar_locatio_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("DCBusbar").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "DCBusbar".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_dc_chopper_locati_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("DCChopper").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "DCChopper".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_dc_ground_locatio_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("DCGround").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "DCGround".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_dc_line_segment_l_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("DCLineSegment").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "DCLineSegment".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_dc_series_device_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("DCSeriesDevice").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "DCSeriesDevice".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_dc_shunt_location_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("DCShunt").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "DCShunt".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_dc_switch_locatio_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("DCSwitch").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "DCSwitch".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_dc_disconnector_l_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("DCDisconnector").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "DCDisconnector".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_dc_breaker_locati_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("DCBreaker").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "DCBreaker".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_dc_line_location_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("DCLine").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "DCLine".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

pub fn check_p61970_600_2_geographicallocation_ap_con_complex_inverseassociation_shacl_dc_converter_unit_etc(dataset: &CimDataset) -> Vec<Violation> {
    let mut ref_counts: std::collections::HashMap<&str, usize> = std::collections::HashMap::new();
    for src_mrid in dataset.by_type.get("Location").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::Location>()) {
            if let Some(r) = &src.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("ServiceLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::ServiceLocation>()) {
            if let Some(r) = &src.base.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    for src_mrid in dataset.by_type.get("WorkLocation").into_iter().flatten() {
        if let Some(src) = dataset.entries.get(src_mrid)
            .and_then(|e| e.element.as_any().downcast_ref::<cimstructs::WorkLocation>()) {
            if let Some(r) = &src.base.power_system_resources { *ref_counts.entry(r.mrid.as_str()).or_insert(0) += 1; }
        }
    }
    let mut violations = Vec::new();
    for mrid in dataset.by_type.get("DCConverterUnit").into_iter().flatten() {
        let count = ref_counts.get(mrid.as_str()).copied().unwrap_or(0);
        if count > 1 {
            violations.push(Violation {
                object_id:   mrid.clone(),
                rule_id:     "gli:PowerSystemResource.Location-cardinality".to_string(),
                class:       "DCConverterUnit".to_string(),
                property:    "PowerSystemResource.Location-cardinality".to_string(),
                message:     "Wrong number of associated instances.".to_string(),
                severity:    "sh:Violation".to_string(),
                name:        "PowerSystemResource.Location-cardinality".to_string(),
                description: "This constraint validates the cardinality of the association at the inverse direction.".to_string(),
            });
        }
    }
    violations
}

