name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    name: Build ${{ matrix.artifact }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            binary: cimcli
            artifact: cimcli-linux-amd64
          - os: windows-latest
            binary: cimcli.exe
            artifact: cimcli-windows-amd64.exe
          - os: macos-13
            binary: cimcli
            artifact: cimcli-darwin-amd64
          - os: macos-latest
            binary: cimcli
            artifact: cimcli-darwin-arm64

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - uses: dtolnay/rust-toolchain@stable

      - uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-release-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-release-

      - name: Generate cimstructs and cimvalidation
        run: make generate

      - name: Build
        run: cargo build --release -p cimoxide-cli

      - name: Stage artifact
        shell: bash
        run: cp target/release/${{ matrix.binary }} ${{ matrix.artifact }}

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.artifact }}

  release:
    name: Create GitHub Release
    if: startsWith(github.ref, 'refs/tags/')
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          merge-multiple: true

      - name: Create release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "${{ github.ref_name }}" \
            --title "Release ${{ github.ref_name }}" \
            --generate-notes \
            --repo ${{ github.repository }} \
            cimcli-linux-amd64 \
            cimcli-windows-amd64.exe \
            cimcli-darwin-amd64 \
            cimcli-darwin-arm64
